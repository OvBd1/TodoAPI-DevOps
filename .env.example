# Modèle du fichier /srv/todo/.env de la machine cible.
# Le vrai .env est copié une fois à la main et ne sort jamais du dépôt.
POSTGRES_DB=todo
POSTGRES_USER=todo
POSTGRES_PASSWORD=remplacer-par-un-mot-de-passe-genere
